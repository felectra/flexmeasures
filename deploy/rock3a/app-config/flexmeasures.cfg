# FlexMeasures config file (valid Python), mounted into the container at /app/instance/flexmeasures.cfg.
# Use it ONLY for settings that have no environment variable. Secrets stay in .env, never here
# — this file is committed to git.

# Default map bounding box, used on the dashboard before any located assets exist.
# Two opposite corners as (latitude, longitude) — this one covers the Mykolaiv region.
FLEXMEASURES_DEFAULT_BOUNDING_BOX = ((48.2, 30.2), (46.4, 33.2))
